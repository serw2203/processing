apiVersion: v1
kind: Service
metadata:
    name: processing-service
    namespace: default
    labels:
        app: processing
spec:
    type: ClusterIP
    selector:
        app: processing
    ports:
        - port: 8080
          name: http
          targetPort: 8080
          protocol: TCP
