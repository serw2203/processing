apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: processing-virtual-service-bootcom
  namespace: default
spec:
  hosts:
    - boot.com
  gateways:
    - ingress-gateway
  http:
    - route:
        - destination:
            host: istio-egressgateway.istio-system.svc.cluster.local
            port:
              number: 11443
          weight: 100